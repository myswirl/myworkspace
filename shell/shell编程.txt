1.什么是shell？
简单地讲，就是命令解析器，将用户输入的指令转换为相应的机器能够运行的程序。

2.shell脚本
shell脚本是一个包含一系列命令序列的文本文件。当运行这个脚本时，文件中包含的命令序列将得到执行。

注意：
1.变量赋值时，等号两端不能有空格；
2.BASH中的语句结尾不需要分号；


plus.sh:
	打印10以内的加法表。

cpufreq.sh:
	设置CPU的主频为固定的。